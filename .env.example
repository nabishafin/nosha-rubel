# The single hostname Google should index. Do not include a path or trailing slash.
SITE_URL=https://nooshaaubel.com

# Optional HTML verification token from Google Search Console (not the full meta tag).
GOOGLE_SITE_VERIFICATION=
